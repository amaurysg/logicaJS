/* function Perros(nombre, edad, estado) {
  this.nombre = nombre
  this.edad = edad
  this.estado = estado

}

var p1 = new Perros("Amaury", 28, "Vivo")
var p2 = new Perros("Julio", 25, "Muerto")
var p3 = new Perros("Pedro", 28, "Vivo")

console.log(p1, p2, p3)
console.log(Perros) */

var n = ["a", "m", "a", "u", "r", "y"]
var contA = 0
var final = {}

console.log(contA)


/* function contarLetras(letras) {
  var objeto = {};
  for (var i in letras) {
    objeto[letras[i]] = (objeto[letras[i]] || 0) + 1; // Incrementamos el valor si el elemento ya existe
  }
  return objeto;
}
console.log(contarLetras("amaury julio soto garay")) */